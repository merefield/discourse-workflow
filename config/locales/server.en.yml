en:
  site_settings:
    workflow_enabled: "Enable the workflow plugin"
  discourse_workflow:
    topic_transition_action_description: 'Moved to step: "%{workflow_step_name}" with action: "%{step_option_name}" by @%{username}'
    errors:
      no_midway_error: "You cannot create a Topic midway through a workflow and this Category is part of a workflow."
    options:
      start: "Start"
      ready: "Ready"
  activerecord:
    errors:
      models:
        workflow:
          attributes:
            name:
              blank: "A workflow must have a name"
        workflow_step:
          attributes:
            name:
              blank: "A workflow step must have a name"
            category_id:
              blank: "You must select a Category"
