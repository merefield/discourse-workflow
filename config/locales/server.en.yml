en:
  site_settings:
    workflow_enabled: "Enable the workflow plugin"
    workflow_overdue_days_default: "Default overdue threshold in days for workflow topics. Use 0 to disable overdue behavior by default."
  discourse_workflow:
    topic_transition_action_description: 'Moved from "%{starting_step_name}" to "%{ending_step_name}" with action: "%{step_option_name}" by @%{username}'
    errors:
      no_midway_error: "You cannot create a Topic midway through a workflow and this Category is part of a workflow."
      transition_failed_stale_state_refreshing: "Transition Failed: probably due to stale UI state - please try again after refresh - refreshing!"
    options:
      start: "Start"
      ready: "Ready"
  activerecord:
    errors:
      models:
        workflow:
          attributes:
            name:
              blank: "A workflow must have a name"
        workflow_step:
          attributes:
            name:
              blank: "A workflow step must have a name"
            category_id:
              blank: "You must select a Category"
